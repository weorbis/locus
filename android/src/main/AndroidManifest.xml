<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.locus">

    <!-- Required permissions (merged into the host app) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <!-- Receives ActivityRecognition updates and forwards them to the plugin -->
        <receiver
            android:name="dev.locus.ActivityRecognizedBroadcastReceiver"
            android:exported="false" />

        <!-- Receives Geofence transitions -->
        <receiver
            android:name="dev.locus.GeofenceBroadcastReceiver"
            android:exported="false" />

        <!-- Receives notification action button clicks -->
        <receiver
            android:name="dev.locus.NotificationActionReceiver"
            android:exported="false" />

        <receiver
            android:name="dev.locus.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Foreground service used for continuous background monitoring -->
        <service
            android:name="dev.locus.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <service
            android:name="dev.locus.HeadlessService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>
</manifest>
