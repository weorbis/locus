library;

/// Types of geolocation events.
enum EventType {
  location,
  motionChange,
  providerChange,
  activityChange,
  geofence,
  geofencesChange,
  heartbeat,
  schedule,
  connectivityChange,
  powerSaveChange,
  enabledChange,
  notificationAction,
  http,
  unknown,
}
